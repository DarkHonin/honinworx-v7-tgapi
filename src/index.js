

/**
 * tdLibControler abstract
 *
 */

const defaultPrams = {
	apiId : process.env.tg_app_id,
	apiHash : process.env.tg_app_hash,
	appDir : process.env.tg_app_dir || "./",
	use_message_database
}

module.exports = {}